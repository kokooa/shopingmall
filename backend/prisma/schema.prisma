// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}


// 1. 기존 User 모델에 아래 한 줄 추가 (내 장바구니 연결)
model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  password  String
  name      String?
  createdAt DateTime @default(now())

  cart      Cart?    // 👈 추가: 유저는 장바구니를 가질 수도, 안 가질 수도 있음 (1:1)
}

// 2. 기존 Product 모델에 아래 한 줄 추가 (누군가의 장바구니에 담김)
model Product {
  id          Int      @id @default(autoincrement())
  name        String
  price       Int
  category    String
  imageUrl    String
  description String?
  createdAt   DateTime @default(now())

  cartItems   CartItem[] // 👈 추가: 이 상품은 여러 장바구니에 담길 수 있음 (1:N)
}

// ==========================================
// 👇 3. 아래 모델들을 파일 맨 밑에 새로 추가하세요
// ==========================================

// 장바구니 (유저당 1개)
model Cart {
  id        Int        @id @default(autoincrement())
  userId    Int        @unique // ⭐ 유저 한 명당 장바구니는 하나만 (Unique)
  user      User       @relation(fields: [userId], references: [id])
  items     CartItem[] // 장바구니 안에 담긴 아이템들
  updatedAt DateTime   @updatedAt // 마지막으로 수정한 시간
}

// 장바구니 아이템 (어떤 상품을 몇 개 담았는지)
model CartItem {
  id        Int      @id @default(autoincrement())
  
  // 어느 장바구니 소속인지
  cartId    Int
  cart      Cart     @relation(fields: [cartId], references: [id])

  // 무슨 상품인지
  productId Int
  product   Product  @relation(fields: [productId], references: [id])

  // 몇 개 담았는지
  quantity  Int      @default(1)

  createdAt DateTime @default(now())
}